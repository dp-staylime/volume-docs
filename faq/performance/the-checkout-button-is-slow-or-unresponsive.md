# The Checkout button is slow or unresponsive

This is usually caused by third-party scripts blocking the browser's main thread.

The theme is rarely the cause.

Check **Settings → Customer Events** and remove any unused tracking pixels.

Disabling an app does not always remove its Web Pixel automatically.
